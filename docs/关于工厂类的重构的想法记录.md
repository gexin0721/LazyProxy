前提
    当前的代码，有且仅仅只能支持个位数的AI的链接，且不能支持高并发，这个是最为关键的，当前OpenAI类和历史类已经完成了重构
解决
    1、在一切的编写，我们需要构建一个新的数据结构，我首先想到是，一套AI一个表，然后一个个表，组成一个map，接着我们用一个数组串起不同的AI，这样我们就可以支持快速查询使用，快速插入删除
        其结构大致为
        [deepseek：AI,]
